# Rustワークスペース定義。
[workspace]
resolver = "2"
members = ["crates/engine-core", "apps/native-host", "apps/desktop/src-tauri"]

[workspace.package]
edition = "2021"
license = "MIT"
publish = false

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
